<!-- nextly:managed:start -->
<!--
  Everything between these markers is generated by create-nextly-app and may be
  replaced when the project is upgraded. Write your own notes OUTSIDE the block:
  anything above or below it is yours and is preserved.
-->

# Agent guide for this Nextly project

A Next.js app with [Nextly](https://nextlyhq.com) for content. The CMS runs
inside this app rather than beside it: same server, same database, no separate
service to start.

## Where things are

| path                   | what it is                                                         |
| ---------------------- | ------------------------------------------------------------------ |
| `nextly.config.ts`     | the config the whole app boots from — collections, database, admin |
| `src/collections/`     | code-first collection definitions, when this project uses them     |
| `src/app/(frontend)/`  | the public site                                                    |
| `src/app/admin/`       | the admin panel, served at `/admin`                                |
| `migrations/`          | SQL migrations, one file per dialect variant                       |
| `src/types/generated/` | generated types — never edit by hand                               |

## Commands

```bash
npm run dev                  # start Next.js; schema changes apply in-process
npm run build                # migrate, then build
npm run db:setup             # first run: create the schema
npm run db:migrate           # apply pending migrations
npm run db:migrate:status    # what has and has not been applied
npm run db:migrate:fresh     # drop everything and re-run (destructive)
npm run types:generate       # regenerate types after a schema change
```

This project uses **{{databaseDialect}}**.

## Reading content

Content is read through the Direct API, on the server:

```ts
import { getNextly } from "nextly";
import nextlyConfig from "@nextly-config";

const nextly = await getNextly({ config: nextlyConfig });
const posts = await nextly.find({ collection: "posts", limit: 10 });
```

**`find()` returns loosely-typed documents until types are generated.** Without
`npm run types:generate`, `posts.items` is `Record<string, unknown>[]` and a
field access is `unknown`. Generating types makes the slug and the return type
infer, so `posts.items` becomes `Post[]`.

Until then, narrow with a runtime check rather than an assertion — a document
missing a field yields `undefined`, and asserting the type only moves the
failure to where the value is used:

```ts
const slug = typeof doc.slug === "string" ? doc.slug : undefined;
```

## Things that surprise people

- **Users are not a normal collection.** Read them through `nextly.users`, not
  `find({ collection: "users" })`. The users namespace supports pagination and
  `search`; it does not support a `where` filter.
- **A migration you generate is one file, in THIS project's dialect.**
  `npx nextly migrate:create <name>` (there is no npm script for it) writes a
  single `migrations/<timestamp>_<slug>.sql` containing **{{databaseDialect}}**
  SQL — no `.mysql.sql` or `.sqlite.sql` beside it. Edit that file.
- **A suffixed set means the migration was shipped, not generated.** A template
  that supports every dialect ships `0001_x.sql` (Postgres) alongside
  `0001_x.mysql.sql` and `0001_x.sqlite.sql`, and the CLI runs the variant for
  the configured dialect. Those are prebuilt. Adding companion variants to a
  migration you generated is not part of the normal workflow, and nothing will
  run them unless this project is itself meant to support several dialects.
- **The admin needs a first user.** Visit `/admin` after `npm run dev` and
  complete the setup screen.
- **Cache tags come from `nextly/runtime`.** Wrap PUBLIC reads in `cachedFind`
  and tag them with `nextlyTags(collection)` so a write invalidates the right
  pages.

  **`keyParts` is a security boundary, not a performance detail.** Two callers
  whose `keyParts` match share one cache entry. So if the read applies any
  per-caller rule — owner-only scoping, role-based visibility, an API key's
  narrowed scope — the caller's identity has to be IN the key, or one user's
  rows get served to another:

  **A read is only "public" if the QUERY makes it so.** `find()` defaults to
  `overrideAccess: true` — a trusted server read that bypasses access rules — so
  filtering by slug alone can return a draft, or a row the rules would have
  hidden, and then cache it for every visitor. Constrain the query explicitly:

  ```ts
  // Public: `status: "published"` is enforced even on a trusted read, so drafts
  // cannot reach the shared entry. Same result for every reader → stable key.
  cachedFind(
    () =>
      nextly.find({
        collection: "posts",
        where: { slug: { equals: slug } },
        status: "published",
      }),
    { tags: nextlyTags("posts"), keyParts: ["posts", slug] }
  );

  // Per-user: evaluate the access rules as the CALLER, and put their identity in
  // the key — the entry is no longer the same for every reader.
  cachedFind(
    () =>
      nextly.find({ collection: "orders", user, overrideAccess: false }),
    { tags: nextlyTags("orders"), keyParts: ["orders", "list", user.id] }
  );
  ```

  If the collection carries per-row access rules, a stable key is wrong even when
  the route looks public. When in doubt, do not cache: a missing cache is slow, a
  shared one leaks.

## Conventions worth keeping

- Server components read content directly; do not build an API route to fetch
  your own content.
- Regenerate types in the same change as the schema edit that needs them.
- Migrations are append-only once shared. Add a new one rather than editing an
  applied file.

<!-- nextly:managed:end -->
