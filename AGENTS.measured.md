<!-- GENERATED by scripts/measure-facts.mjs — do not edit. -->
<!-- Regenerate: node scripts/measure-facts.mjs · run at 2026-08-19T15:50:44.230Z -->

# Measured facts

Perishable numbers referenced by AGENTS.md. Every row names its command so
a doubtful reader re-runs it instead of trusting the file. Heavy rows need
`--full` and a tree whose cache state you know — a warm cache overstates
health and a missing `dist` overstates breakage.

## packages

packages in the workspace

```
$ ls packages | wc -l
25: adapter-drizzle, adapter-mysql, adapter-postgres, adapter-sqlite, admin, admin-css, blocks-engine, blocks-react, builder, create-nextly-app, eslint-config, eslint-plugin, module-specifiers, nextly, plugin-form-builder, plugin-page-builder, plugin-sdk, plugin-seo, prettier-config, storage-s3, storage-uploadthing, storage-vercel-blob, telemetry, tsconfig, ui
```

## pr-scopes

PR scopes accepted by the title check (the enforced list; commitlint checks format only, not scope membership)

```
$ sed -n '/scopes: |/,/requireScope/p' .github/workflows/pr-title.yml
nextly, admin, client, ui, adapter-postgres, adapter-mysql, adapter-sqlite, adapter-drizzle, storage-s3, storage-vercel-blob, storage-uploadthing, blocks-engine, blocks-react, builder, plugin-form-builder, plugin-page-builder, plugin-seo, plugin-sdk, create-nextly-app, eslint-config, eslint-plugin, prettier-config, tsconfig, telemetry, playground, root, ci, docs, deps, release
```

## engines

supported node ranges and the pinned pnpm

```
$ node -e "const p=require('./package.json');console.log(p.engines.node, p.packageManager)"
node ^20.19.0 || ^22.12.0 || >=24.0.0 · pnpm@9.0.0
```

## comment-convention

comment-convention baseline (allowlisted pre-existing offences)

```
$ node scripts/check-comment-convention.mjs | tail -1
4040 of 4064 source files across ., packages, apps, e2e, templates, scripts: no new comment names a review, tool or change; 24 read with the pull-request vocabulary allowed; 402 pre-existing offence(s) still allowlisted.
```

## check-types-cold

check-types on a clean tree, cache forced off (the honest failure count before a build)

```
$ pnpm turbo run check-types --continue --force
(not measured in this run — heavy; re-run with --full)
```

## lint-cold

lint on a clean tree, cache forced off

```
$ pnpm turbo run lint --continue --force
(not measured in this run — heavy; re-run with --full)
```
