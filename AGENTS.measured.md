<!-- GENERATED by scripts/measure-facts.mjs — do not edit. -->
<!-- Regenerate: node scripts/measure-facts.mjs --full · run at 2026-08-19T16:08:51.978Z -->

# Measured facts

Perishable numbers referenced by AGENTS.md. Every row shows the command that
actually ran and its exit status, so a doubtful reader re-runs instead of
trusting and a failed measurement cannot read as a number. Heavy rows need
`--full`; their meaning depends on the build state recorded beside them.

## packages

packages in the workspace

```
$ ls packages | tr '\n' ' '
adapter-drizzle adapter-mysql adapter-postgres adapter-sqlite admin admin-css blocks-engine blocks-react builder create-nextly-app eslint-config eslint-plugin module-specifiers nextly plugin-form-builder plugin-page-builder plugin-sdk plugin-seo prettier-config storage-s3 storage-uploadthing storage-vercel-blob telemetry tsconfig ui
```

## pr-scopes

PR scopes accepted by the title check (the enforced list; commitlint checks format only, not scope membership)

```
$ sed -n '/scopes: |/,/requireScope/p' .github/workflows/pr-title.yml | sed '1d;$d' | tr -d ' ' | tr '\n' ' '
nextly admin client ui adapter-postgres adapter-mysql adapter-sqlite adapter-drizzle storage-s3 storage-vercel-blob storage-uploadthing blocks-engine blocks-react builder plugin-form-builder plugin-page-builder plugin-seo plugin-sdk create-nextly-app eslint-config eslint-plugin prettier-config tsconfig telemetry playground root ci docs deps release
```

## engines

supported node ranges and the pinned pnpm

```
$ node -e "const p=require('./package.json');console.log(p.engines.node, '·', p.packageManager)"
^20.19.0 || ^22.12.0 || >=24.0.0 · pnpm@9.0.0
```

## comment-convention

comment-convention baseline (allowlisted pre-existing offences)

```
$ node scripts/check-comment-convention.mjs 2>&1 | tail -1
4040 of 4064 source files across ., packages, apps, e2e, templates, scripts: no new comment names a review, tool or change; 24 read with the pull-request vocabulary allowed; 402 pre-existing offence(s) still allowlisted.
```

## check-types-cold

check-types with the turbo cache forced off (read the dist-state line before interpreting)

```
$ pnpm turbo run check-types --continue --force
6 of 22 successful  [exit 1]
dist-state at measurement: 0 of 25 packages have dist/
```

## lint-cold

lint with the turbo cache forced off (read the dist-state line before interpreting)

```
$ pnpm turbo run lint --continue --force
9 of 24 successful  [exit 1]
dist-state at measurement: 0 of 25 packages have dist/
```
