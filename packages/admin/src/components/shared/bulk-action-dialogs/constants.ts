/**
 * Bulk Operations Dialog Constants
 *
 * Extracted constants to avoid magic numbers and improve maintainability.
 */

/**
 * Maximum number of users to display in dialog lists before showing "...and X more"
 * @default 5
 */
export const MAX_VISIBLE_USERS = 5;

/**
 * Maximum height for scrollable user lists in dialogs (px)
 * @default 300
 */
export const MAX_USER_LIST_HEIGHT = 300;
