{
  "name": "@nextlyhq/admin-css",
  "version": "0.0.2-alpha.35",
  "description": "Build tooling to scope and compile Nextly admin CSS (shared by the admin build and third-party plugins).",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs"
  },
  "bin": {
    "nextly-build-admin-css": "./bin/nextly-build-admin-css.mjs"
  },
  "files": [
    "src",
    "bin"
  ],
  "scripts": {
    "test": "vitest run",
    "check-types": "tsc --noEmit || true",
    "lint": "eslint ."
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
