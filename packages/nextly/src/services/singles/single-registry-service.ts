/**
 * Re-export stub — the implementation has moved to
 * `src/domains/singles/services/single-registry-service.ts`.
 *
 * This file is preserved to keep legacy import paths working during the
 * New code should import directly from
 * `nextly` or the domain path.
 */

export {
  SingleRegistryService,
  type UpdateSingleOptions,
  type DeleteSingleOptions,
  type CodeFirstSingleConfig,
  type SyncSingleResult,
  type ListSinglesOptions,
  type ListSinglesResult,
} from "../../domains/singles/services/single-registry-service";
