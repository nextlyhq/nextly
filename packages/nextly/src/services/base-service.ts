/**
 * BaseService — Re-export
 *
 * BaseService has moved to `src/shared/base-service.ts`. This file
 * re-exports it so existing imports continue to work.
 *
 * @module services/base-service
 * @since 1.0.0
 */
export { BaseService } from "../shared/base-service";
