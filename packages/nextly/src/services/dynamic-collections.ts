/**
 * @deprecated Moved to `src/domains/dynamic-collections/services/dynamic-collection-service.ts`.
 * This stub re-exports the new location so legacy imports keep working.
 *
 * Note: Callers that import from `"../services/dynamic-collections"` resolve
 * to this file (TypeScript prefers `.ts` over `/index.ts`), so we re-export
 * the full domain surface here.
 */
export * from "../domains/dynamic-collections";
