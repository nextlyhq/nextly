/**
 * Shared Service Types — Re-export
 *
 * Types have moved to `src/shared/types/`. This file re-exports them
 * so existing imports (`from "../services/shared"`) continue to work.
 *
 * @module services/shared/types
 * @since 1.0.0
 */
export * from "../../shared/types";
