/**
 * Schema Services — Re-export
 *
 * Services have moved to `src/domains/schema/`. This file re-exports them
 * so existing imports (`from "../services/schema"`) continue to work.
 *
 * @module services/schema
 * @since 1.0.0
 */
export * from "../../domains/schema";
