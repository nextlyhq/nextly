/**
 * Demo data prompt configuration.
 *
 * Labels for the confirm prompt that asks whether to include
 * sample posts, authors, and images in the scaffolded project.
 */

export const DEMO_DATA_LABELS = {
  active: "Yes - Sample posts, authors, and images to explore",
  inactive: "No - Start with empty collections",
} as const;
