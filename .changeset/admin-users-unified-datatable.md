---
"@nextlyhq/admin": patch
---

The Users list now uses the unified admin data table. Clicking anywhere on a row (not just the name) opens the user, per-row actions live in a consistent menu, and the list collapses to a readable card layout on narrow screens. Selection, bulk delete, search, column visibility, and pagination are unchanged.
