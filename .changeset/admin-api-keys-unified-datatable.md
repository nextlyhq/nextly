---
"@nextlyhq/admin": patch
---

The API keys list now uses the unified admin data table. Clicking a row opens the edit dialog for active keys, actions live in a consistent menu, and the type badges follow the monochrome theme instead of hard-coded colors. Revoked keys stay read-only. Search, column visibility, and pagination are unchanged.
