---
"@revnixhq/admin": minor
---

Clean up the media-library edit popup: drop the tags field (UI only — backend untouched), hide the crop / focal-point / image-sizes UI behind an "Edit advanced options" toggle so the dialog is short by default, and normalize form-field styling to use theme tokens consistently.
