---
"@nextlyhq/admin": patch
---

The API Keys create page now uses the shared settings section/row layout (label and help on the left, control on the right) instead of a narrow left-aligned column inside a full-width card. This removes the large empty space on the right and makes the page visually consistent with the General Settings and Image Sizes pages.
