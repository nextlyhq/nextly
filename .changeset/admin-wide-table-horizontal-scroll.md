---
"@nextlyhq/admin": patch
---

Wide admin tables now keep readable column widths and scroll horizontally when they exceed the available width, instead of shrinking every column to fit (which cut off or cramped the last columns on tablet). Narrow tables still fill their container with no scroll, and the mobile card view is unchanged. Applies to the shared data table and the user-fields table.
