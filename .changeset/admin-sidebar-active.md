---
"@nextlyhq/admin": patch
---

Make sidebar navigation clearer. The active nav item (in both the icon rail and the inner sidebar) now uses a visible muted background instead of a ~5%-opacity tint, and resting items have stronger contrast, so the current section is obvious at a glance in both light and dark mode.
