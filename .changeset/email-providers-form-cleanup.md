---
"@revnixhq/admin": minor
---

Restructure the Email Provider create/edit form: drop the right-side sidebar, move action buttons (Test Connection, Cancel, Create/Update) into the page header to match other settings pages, move "Set as Default Provider" inline into the form body, drop the duplicated in-form page header, and adopt the shared SettingsSection / SettingsRow primitives so the layout matches the rest of the settings area.
