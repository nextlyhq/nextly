---
"@nextlyhq/admin": patch
---

Cleaned up two more admin lists. Built-in rows on the User Fields page no longer have a dulled gray background: they read as normal rows, with the lock icon and Built-in badge still marking them read-only. The Image Sizes settings list now uses the unified admin data table, with whole-row navigation to edit, a consistent action menu, and a card layout on narrow screens; config-defined sizes stay non-deletable.
