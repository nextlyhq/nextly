---
"@nextlyhq/admin": patch
---

Custom rows on the User Fields page are now clickable across the whole row, opening the field editor, so they behave like every other admin list. Built-in fields stay read-only, and the drag handle and row actions menu no longer trigger navigation.
