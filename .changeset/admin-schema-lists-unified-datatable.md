---
"@nextlyhq/admin": patch
---

The singles, components, and plugins lists now use the unified admin data table, matching the collections list. Rows navigate on whole-row click (singles and components open in the builder), actions live in a consistent menu, and the lists collapse to cards on narrow screens. Locked code-first singles and components stay read-only, with their selection checkbox disabled and excluded from bulk delete. Search, filters, column visibility, and pagination are unchanged; the plugins list remains a read-only preview.
