---
"@revnixhq/admin": patch
---

Document tabs above the action bar on the entry / single create + edit pages: Edit · API · Versions (Soon) · Live Preview (Soon). The API and Compare pages now also show the same strip so navigating between Edit and API feels like switching tabs on the same document instead of moving to a separate page. Singles render Edit + API only — Versions / Live Preview are reserved for later.
