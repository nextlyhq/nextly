---
"@revnixhq/admin": minor
---

Move the Email Layout Framework (global header/footer HTML) from a collapsible card on the templates list to a dedicated page at `/admin/settings/email-layout`, accessible via a new "Layout" entry in the settings sidebar under Email Configuration. Layout editor uses the same Prism-based HTML syntax highlighting as the template editor.
