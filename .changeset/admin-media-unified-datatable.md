---
"@nextlyhq/admin": patch
---

The media library list view now uses the unified admin data table. Clicking a row still opens the media edit popup (it does not navigate away), thumbnails and file details render as before, and the list collapses to cards on narrow screens. The grid/list toggle, selection, bulk actions, folder sidebar, and column visibility are unchanged.
