---
"@revnixhq/admin": patch
---

Image Sizes settings list adopts the shared SettingsTableToolbar; standardizes search bar tokens. The Add-Size dialog itself is intentionally unchanged (separate task).
