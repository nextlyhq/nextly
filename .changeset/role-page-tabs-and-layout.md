---
"@revnixhq/admin": minor
---

Restructure the Create Role / Edit Role pages: stack Role Details and Permissions sections vertically instead of side-by-side, normalize the permissions tabs to use the shared Tabs component, fix the cut-off permissions search bar, and adopt the shared SettingsSection layout primitives.
