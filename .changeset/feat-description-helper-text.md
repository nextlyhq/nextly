---
"@revnixhq/admin": patch
---

Field descriptions on entry create/edit pages now render as helper text
directly below each field's input — always visible, no hover required —
replacing the previous info-icon tooltip pattern. Group, Component, and
Repeater container descriptions share the same `text-xs
text-muted-foreground leading-relaxed` typography for visual consistency
across collection, single, and component contexts.
