---
"@revnixhq/admin": patch
---

Email Templates settings list adopts the shared SettingsTableToolbar; standardizes search bar tokens. The Email Layout Framework section above the table is intentionally untouched (separate task).
