---
"@revnixhq/admin": minor
---

Restructure the Email Template create/edit form to match the Email Providers cleanup: action buttons live in the page header (no duplicate in-form heading), shared SettingsSection / SettingsRow primitives for layout. HTML Content field now uses syntax highlighting via prismjs + react-simple-code-editor for a much better authoring experience.
