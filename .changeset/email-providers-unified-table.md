---
"@nextlyhq/admin": patch
---

Render the email providers list with the unified admin data table instead of the older responsive table, matching the templates list and every other list in the admin. Clicking a row opens the provider; Edit / Set Default / Send Test / Delete move into the shared row-action menu, and the default-provider star now uses a theme token instead of a hardcoded colour.
