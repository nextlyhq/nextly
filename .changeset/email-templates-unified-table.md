---
"@nextlyhq/admin": patch
---

Render the email templates list with the unified admin data table instead of the older responsive table, so it matches every other list in the admin (consistent header, row interactions, row-action menu, and pagination). Clicking a row opens the template; Edit / Preview / Duplicate / Delete move into the shared row-action menu (Delete stays hidden for built-in templates). The list preview now uses realistic sample data so template variables render instead of showing the wrong built-in names.
