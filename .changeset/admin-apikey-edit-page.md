---
"@nextlyhq/admin": patch
---

Editing an API key now opens a full page instead of a small modal. The edit page uses the same settings layout as the create page: the name and description are editable, and the immutable properties (key prefix, token type, expiry, created date) are shown in a read-only section for context. Clicking a key in the list navigates to this page.
