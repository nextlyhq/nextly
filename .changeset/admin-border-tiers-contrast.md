---
"@nextlyhq/admin": minor
---

Improve admin visual hierarchy: add a three-tier border scale (`border-subtle` / `border` / `border-strong`) and raise border, input, and table contrast so cards, tables, groups, and dividers are clearly visible in both light and dark mode. Dark-mode cards now sit on a raised surface instead of merging into the page background. Pure monochrome; no accent color introduced.
