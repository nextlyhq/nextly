# RESUME PROMPT — paste this into a fresh session

Continue PR #634 (Nextly admin theme). **The theme is chosen, applied and pushed. The work left is
closing out review findings so the PR can merge.**

## Step 0 — read first, in this order

1. **`apps/playground/src/theme-lab/HANDOFF-2026-08-11-pr634.md` IN FULL.** §5 is the open-thread
   inventory with thread IDs; §6 is the list of things that cost real time.
2. `apps/playground/src/theme-lab/AUDIT-task-08.md` — why the theme changed, and the honest limits
   of the contrast metric.
3. Skim `HANDOFF-2026-08-11-admin-theme.md` only if you need the theme-selection history; it is
   superseded and one of its instructions is now wrong (see §6.7).

## Step 1 — orient with real values

```
cd /Users/mobeen/Work/Products/nextly-integrations/nextly-worktrees/theme-variations
git fetch origin && git status --short && git log --oneline -3
git ls-remote --heads origin explore/admin-theme-variations
gh pr view 634 --repo nextlyhq/nextly --json headRefOid,mergeable,mergeStateStatus
gh pr checks 634 --repo nextlyhq/nextly --json name,state -q '.[] | "\(.state)\t\(.name)"'
```

Expect head `3555880d097251b5cc907ed366434e0998e85d2e`, remote identical, `mergeable=MERGEABLE`,
`mergeState=BLOCKED`. `origin/main` moves several times an hour.

Count unresolved threads with the GraphQL `reviewThreads` query — **never infer from CI colour or
review timestamps**:

```
gh api graphql -f query='query { repository(owner:"nextlyhq",name:"nextly"){ pullRequest(number:634){
  reviewThreads(first:100){ nodes { id isResolved path comments(first:1){ nodes { databaseId body } } } } } } }'
```

**Work ONLY in `nextly-worktrees/theme-variations`.** Never in `.../nextly-integrations/nextly`.

## Step 2 — the work, in priority order

### 1. The four shipped-code threads (§5 of the handoff)

Highest value because they touch published packages:

- `PRRT_kwDOSYwUJs6YGJPS` — a **real gap in a test I wrote**: `comments-match-token-values.test.ts`
  pins `:root` against a vacuous scan but not `.dark`. Fix and stub-verify.
- `PRRT_kwDOSYwUJs6YGJPd` — `packages/ui/src/types/table.ts` cites a "spec section 5.1" that does
  not exist. State the contract instead.
- `PRRT_kwDOSYwUJs6YGJPO` / `YGJPQ` — rationale comments on the one-line token substitutions.
  AGENTS.md does require a what/why comment per change, so these are legitimate.
- `PRRT_kwDOSYwUJs6YGMJ-` — **believed already fixed** by `6db4c6ee1`. Verify the page-background
  measurement, then reply and resolve rather than re-fixing.

### 2. The substantive playground threads

Read each in full and MEASURE before changing anything. Several are probably already fixed by later
commits (`PRRT_kwDOSYwUJs6YCLUj` on `table-header-bg` almost certainly is) — verify and resolve
those rather than redoing them.

### 3. The six planning-document threads

Lowest value. Consider replying that the plan is executed and its findings live in code, rather than
editing a historical plan retroactively. Read them first — two mention real behaviours.

### 4. Then

Report readiness to the founder. Merge with `--admin` **only if CI is fully green AND unresolved is
zero** — the founder granted this conditionally and both halves must hold. After merging, send the
`styles/contrast/` all-clear to socket `6711`, then start `TASK-neutral-ramp.md`.

## Step 3 — the loop for every finding

Read in full → **measure the claim against real source** → fix the cause not the symptom → add a
test → **stub-verify it** (disable the mechanism, confirm THAT test fails) → typecheck, lint, run
affected suites → merge `origin/main` → push → **verify with `git ls-remote`** → reply in the thread
→ resolve via GraphQL → post `@codex please review this PR`.

**On stub-verification:** if a stub does not falsify, first check the stub actually landed and
actually disables an input the code reads. Three times this session a non-falsifying stub was a bad
probe rather than an inadequate test. Only after ruling that out is the test at fault.

## Step 4 — talk to the other sessions

`ListAgents`, then `SendMessage`. Lanes and boundaries are in handoff §7. Ask peers for a
**delete-or-regenerate list**, not just an edit list — that question has twice surfaced collisions a
normal exchange missed. State your own holdings and intentions as **separate** lists; conflating
them is how two lanes came to believe this one was running a sweep it never planned.

## Hard rules

Never commit to `main`. Never `--no-verify`. No AI attribution. Conventional Commits, lowercase
imperative ≤72 chars, via `git commit -F`. ONE changeset per PR at `patch` (already present). No
`as any` / `@ts-expect-error` / eslint-disable — type a `.mjs` helper with a sibling `.d.mts`.
Comments describe the CODE, never tasks or conversations — there is a test for this. Never use
"tweakcn" or any preset brand name in user-facing text. After merging main, `pnpm install` before
trusting a build. Never bare `git stash`.

## The judgements that settle most questions here

- **Measure the artifact the product renders**, not the one convenient to measure. Every significant
  finding in this PR is an instance of that being violated.
- **Passing and passing-by-enough are different properties.** There is now a margin gate; solve to a
  margin, never to the threshold.
- **A green assertion covers the pair it NAMES, not the pair the product renders.**
- **An exit code from a pipeline is the last command's.** `| tail` masks failure.
- **`git push` can report a rejection when the push already succeeded.** Settle with `git ls-remote`.
- **`packages/ui` typechecks zero test files.** Do not claim coverage you do not have.
- **Tokenise rather than scan**, and group before anchoring a regex alternation.
